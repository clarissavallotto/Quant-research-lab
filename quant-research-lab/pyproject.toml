[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-research-lab"
version = "0.1.0"
description = "Quantitative research project"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
